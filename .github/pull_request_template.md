## What & why

<!-- One or two sentences: what this PR does and which MASTER_PLAN item it serves. -->

## Screenshots / proof

<!-- UI change: light + dark screenshots. Backend change: test output or a curl transcript. -->

## Checklist

- [ ] `npm run lint`, `npm run typecheck`, and `npm test` pass locally
- [ ] Production build passes (`npm run build`)
- [ ] No secrets, keys, or `.env` values in the diff
- [ ] New logic has tests where it's pure/testable
- [ ] UI follows DESIGN_SYSTEM.md — tokens only, light + dark verified, keyboard accessible, `prefers-reduced-motion` respected
- [ ] MASTER_PLAN.md / CHANGELOG.md updated if this completes a feature
